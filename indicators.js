export function sma(v,p){if(v.length<p)return null;return v.slice(-p).reduce((a,b)=>a+b,0)/p}export function rsi(v,p=14){if(v.length<p+1)return null;let g=0,l=0,s=v.slice(-(p+1));for(let i=1;i<s.length;i++){let d=s[i]-s[i-1];if(d>0)g+=d;else l+=Math.abs(d)}g/=p;l/=p;if(l===0)return 100;return 100-100/(1+g/l)}export function volatility(v,p=30){if(v.length<p)return null;let s=v.slice(-p),r=[];for(let i=1;i<s.length;i++)if(s[i-1])r.push((s[i]-s[i-1])/s[i-1]);let m=r.reduce((a,b)=>a+b,0)/r.length;return Math.sqrt(r.reduce((a,b)=>a+(b-m)**2,0)/r.length)*100}