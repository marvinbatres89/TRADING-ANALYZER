import{rsi,sma,volatility}from"./indicators.js";export function analyze(p){if(p.length<31)return{ready:false,trend:"Esperando",trendHint:`Faltan ${31-p.length} datos`,rsi:null,rsiHint:"Periodo 14",volLabel:"--",volHint:"Calculando",confidence:0,signal:"Esperar datos",type:"wait",explanation:"La plataforma necesita al menos 31 precios para crear una lectura técnica inicial."};let R=rsi(p),V=volatility(p),f=sma(p,7),s=sma(p,21),last=p.at(-1),d=(f-s)/s*100,trend=d>.002?"Alcista":d<-.002?"Bajista":"Lateral",buy=0,sell=0,reasons=[];if(trend==="Alcista"){buy+=35;reasons.push("tendencia alcista")}if(trend==="Bajista"){sell+=35;reasons.push("tendencia bajista")}if(R<35){buy+=30;reasons.push("RSI bajo")}else if(R>65){sell+=30;reasons.push("RSI alto")}else{buy+=10;sell+=10;reasons.push("RSI neutral")}if(last>f)buy+=20;if(last<f)sell+=20;let volLabel=V<.005?"Baja":V<.02?"Media":"Alta";if(volLabel==="Media"){buy+=10;sell+=10}if(volLabel==="Alta"){buy-=5;sell-=5;reasons.push("volatilidad alta")}buy=Math.max(0,Math.min(100,Math.round(buy)));sell=Math.max(0,Math.min(100,Math.round(sell)));let type="wait",signal="Esperar",confidence=Math.max(buy,sell);if(Math.abs(buy-sell)>=20&&buy>=55){type="buy";signal="Sesgo alcista"}else if(Math.abs(buy-sell)>=20&&sell>=55){type="sell";signal="Sesgo bajista"}else confidence=Math.min(confidence,50);return{ready:true,trend,trendHint:trend==="Lateral"?"Medias muy próximas":"Cruce de medias",rsi:R,rsiHint:R<30?"Sobreventa":R>70?"Sobrecompra":"Zona neutral",volLabel,volHint:`${V.toFixed(4)}%`,confidence,signal,type,explanation:type==="wait"?`Los criterios todavía no están alineados: ${reasons.join(", ")}.`:`Lectura basada en ${reasons.join(", ")}. El porcentaje expresa coincidencia, no una ganancia garantizada.`}}